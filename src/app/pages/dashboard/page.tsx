'use client';
import AdminDashboard from '@/components/views/AdminDashboard';

export default function DashboardPage() {
    // Página de dashboard: delega a UI/indicadores para o componente AdminDashboard
    return <AdminDashboard />;
}
