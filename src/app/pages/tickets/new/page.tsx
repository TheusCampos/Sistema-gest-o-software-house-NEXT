'use client';
import TicketForm from '@/components/views/TicketForm';

export default function NewTicketPage() {
    // Página de abertura: apenas renderiza o formulário de ticket em modo "novo"
    return <TicketForm />;
}
